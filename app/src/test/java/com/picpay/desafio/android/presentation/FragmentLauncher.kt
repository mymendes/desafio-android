package com.picpay.desafio.android.presentation

import android.app.Activity
import android.os.Bundle
import androidx.annotation.StyleRes
import androidx.fragment.app.Fragment
import androidx.lifecycle.Lifecycle
import androidx.test.core.app.launchActivity

/**
 * Helper para instanciar um fragment com um theme pré definido.
 */
/*inline fun <reified F : Activity> launchThemedFragment(
    fragmentArgs: Bundle? = null,
    @StyleRes themeResId: Int = R.style.Theme_MaterialComponents_Light,
    initialState: Lifecycle.State = Lifecycle.State.RESUMED,
    crossinline instantiate: () -> F
) {
  //  launchActivity(fragmentArgs, themeResId, initialState, instantiate)
}*/
